/*Добавьте в этот файл пояснения к выбранной модели данных. 
Также вы можете сохранить здесь другие комментарии по проекту, которые вы считаете необходимыми, 
и задать вопросы ревьюеру*/

Изменил код с учетом комментариев.

Название новой таблицы
CREATE TABLE car_shop.brands(
    id SERIAL PRIMARY KEY,
    brand_name VARCHAR NOT NULL unique -- текст, т.к. имя бренда
    );

trim(substr(SPLIT_PART(t.auto, ', ', 1), position(' ' IN SPLIT_PART(t.auto, ', ', 1)) + 1)) - так вытаскиваю название моделей, без цвета и бренда.
Может быть это можно делать лаконичнее, через регулярки. Но мы пока их не изучали.

